import toml from 'toml-js'

function parseToml(str) {
    Promise.all(async () => {
        parseToml(str)
    }).then(msg => {
        return msg
    }).catch(err => {
        throw err;
    })
}

export default parseToml